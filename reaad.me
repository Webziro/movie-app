# Movie App
Full-stack movie app with React frontend and Node.js backend.

## Setup
1. Clone repo.
2. Backend: `cd backend && npm install && npm start`
3. Frontend: `cd frontend && npm install && npm run dev`
4. Env: Copy `.env.example` to `.env` and fill vars.

## Deployment
- Backend: Render (set MONGODB_URI, etc.)
- Frontend: Netlify (set REACT_APP_API_URL)
- DB: MongoDB Atlas