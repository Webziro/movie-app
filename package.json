{
  "name": "react-movie-app",
  "version": "1.0.0",
  "description": "React Movie App with Netlify Functions",
  "scripts": {
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm run dev",
    "install-deps": "cd frontend && npm install && cd ../netlify/functions && npm install"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.20.0",
    "nodemailer": "^6.10.1"
  }
}
